var assert = require('assert');
var password = require('../public/javascripts/checkPasswords.js')

/*
describe('Password-tests', function() {
  it('Test password too short', function() {
    assert.fail(1+1, 8, '1+1!=8');
  });
});
*/
